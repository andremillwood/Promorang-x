# API Configuration
VITE_API_URL=http://localhost:3001

# Application Environment (development, staging, production)
VITE_APP_ENV=development

# JWT Secret (for client-side token validation if needed)
# VITE_JWT_SECRET=your_jwt_secret_here
