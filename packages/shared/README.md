# @promorang/shared

Placeholder package for shared TypeScript services, hooks, and type definitions consumed by both the web and mobile apps. Populate `src/` with extracted modules from `apps/web` before publishing.
