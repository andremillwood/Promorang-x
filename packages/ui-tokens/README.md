# @promorang/ui-tokens

Centralized color, surface, and motion token exports for both the web and mobile clients. Currently mirrors the values defined in `apps/web` Tailwind configuration and will be extended as the RN palette comes online.
