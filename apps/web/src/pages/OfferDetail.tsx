import { useQuery } from '@tanstack/react-query';import { Link,useParams } from 'react-router-dom';import { ArrowLeft,Gift,ShieldCheck } from 'lucide-react';import { API_BASE_URL } from '@/lib/api';import { Button } from '@/components/ui/button';import { useCommerceActions } from '@/hooks/useCommerceActions';
export default function OfferDetail(){const {id}=useParams();const actions=useCommerceActions();const q=useQuery({queryKey:['offer',id],queryFn:async()=>{const r=await fetch(`${API_BASE_URL}/coupons/public/${id}`);const d=await r.json();if(!r.ok)throw new Error(d.message||'Offer unavailable');return d.data?.coupon||d.coupon||d.data}});const x=q.data;if(q.isLoading)return <div className="p-10 text-center">Opening offer…</div>;if(!x)return <div className="p-10 text-center">Offer unavailable.</div>;const value=x.discount_value||x.value;return <main className="mx-auto max-w-4xl px-4 py-7"><Link to="/for-you" className="inline-flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft className="h-4 w-4"/>Feed</Link><section className="mt-5 overflow-hidden rounded-[2rem] border border-primary/25 bg-[radial-gradient(circle_at_85%_5%,rgba(255,106,26,.3),transparent_32%),#0a0a0a] p-7 text-white sm:p-11"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-primary text-black"><Gift className="h-6 w-6"/></div><p className="mt-8 text-[10px] font-black uppercase tracking-[.24em] text-primary">Claimable reward</p><h1 className="mt-3 text-5xl font-black tracking-[-.055em] sm:text-7xl">{x.title||x.name}</h1><p className="mt-5 max-w-2xl text-base leading-7 text-white/60">{x.description}</p><div className="mt-8 flex flex-wrap items-end justify-between gap-5"><div><p className="text-4xl font-black">{value}{x.discount_type==='percentage'||x.value_unit==='percentage'?'%':''}</p><p className="text-xs text-white/40">{x.end_date?`Available until ${new Date(x.end_date).toLocaleDateString()}`:'While available'}</p></div><Button size="lg" disabled={!!actions.busy} onClick={()=>actions.claim(String(id))}>{actions.busy?'Claiming…':'Claim to wallet'}</Button></div><p className="mt-7 flex items-center gap-2 text-xs text-white/40"><ShieldCheck className="h-4 w-4"/>Eligibility is checked before a redemption code is issued.</p></section></main>}
